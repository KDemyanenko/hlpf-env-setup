## Student
- Name: ПІБ
- Group: ГРУПА

```text
docker --version
docker compose version
docker run --rm hello-world

(встав тут реальний вивід)

docker compose run --rm npm npm -v
docker compose run --rm npm node --version
```

## Repository structure

Dockerfile  
docker-compose.yml  
README.md